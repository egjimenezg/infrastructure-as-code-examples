name: gh-pages-dns
description: Pulumi project to point a custom domain to GitHub Pages using Amazon Route53.
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
